<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <!-- region Root -->

    <LinearLayout
        android:id="@+id/rootView"
        android:layout_width="match_parent"
        android:layout_height="@dimen/size_p80"
        android:focusable="true"
        android:clickable="true"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:clipToPadding="false"
        android:layout_marginStart="@dimen/size_p8"
        android:layout_marginBottom="@dimen/size_p4"
        >

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/paymentStatusBg"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            >

            <ImageView
                android:id="@+id/avater"
                android:layout_width="@dimen/size_p72"
                android:layout_height="@dimen/size_p72"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"/>

        </androidx.constraintlayout.widget.ConstraintLayout>

        <TextView
            style="@style/Ugo.TextAppearance.Text.B1"
            android:id="@+id/githubUser"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            tools:text="githubUser"
            android:layout_marginStart="@dimen/size_p6"
            />

    </LinearLayout>

    <!-- endregion -->

</layout>
